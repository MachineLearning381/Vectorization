import numpy as np
import datums as dat

resloop = []
xtrav = 0

for x in dat.matrix:
    res.append([])
    inc = 0
    while inc < len(dat.matrix)
        ytrav = 0
        val = 0
        for y in x:
            val += (dat.matrix[xtrav][ytrav] - dat.matrix[inc][ytrav]) ** 2
        resloop[inc].append(val ** .5)
        inc++

resnump = np.array(dat.matrix)